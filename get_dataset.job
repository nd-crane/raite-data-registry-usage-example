#!/bin/bash
#$ -M pmoreira@nd.edu
#$ -m abe

# Go to the path where you want the data to be downloaded
cd /project01/cvrl/pmoreira/raite-usage-example

# load your virtual environment with DVC installed and run `dvc get` on the RAITE data registry repo.
#   (In my case, I'm using PDM to manage the python packages, including DVC)
pdm run dvc get https://github.com/nd-crane/raite-data-registry


